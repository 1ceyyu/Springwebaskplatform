
# 🚀 SpringWeb 问答交互平台 (SpringWebAskPlatform)

一个基于 Spring Boot 3 和 Thymeleaf 开发的轻量级问答社区项目，实现了异步实时数据更新、多行文本格式保留以及动态时间戳展示。

---

## 🌐 访问地址
- **项目入口**：http://10.100.164.26:8082/login

---

## 📂 项目结构

```
Springwebaskplatform/
├── src/main/java/com/example/springwebaskplatform/
│   ├── controller/
│   │   ├── AuthController.java      # 用户认证与登录注册逻辑
│   │   ├── CaptchaController.java   # 验证码生成与验证
│   │   └── QuestionController.java  # 问题展示、发布及异步 JSON 接口
│   ├── model/
│   │   ├── Question.java            # 问题实体类（包含标题、作者、内容、回复列表）
│   │   ├── Reply.java               # 回复实体类（包含时间戳处理 getFormattedTime）
│   │   └── User.java                # 用户实体类（用户认证信息）
│   ├── repository/
│   │   ├── QuestionRepository.java  # 问题数据访问层
│   │   └── UserRepository.java      # 用户数据访问层
│   ├── service/
│   │   ├── QuestionService.java     # 问题相关业务逻辑
│   │   └── UserService.java         # 用户相关业务逻辑
│   └── SpringwebaskplatformApplication.java  # 项目启动类
├── src/main/resources/
│   ├── static/css/                  # 静态CSS样式文件
│   ├── templates/                   # Thymeleaf 页面模版
│   │   ├── login.html               # 登录页面
│   │   ├── register.html            # 注册页面
│   │   ├── question_list.html       # 问题列表页（含列表轮询脚本）
│   │   ├── question_detail.html     # 问题详情页（含回复轮询与 DOM 更新脚本）
│   │   └── add_question.html        # 发布问题页面
│   └── application.properties       # 项目配置文件（端口、应用名称等）
└── pom.xml                          # Maven 项目配置文件
```

---

## ✨ 核心功能

- **用户认证系统**：完整的登录注册功能，支持密码加密存储和验证码验证
- **实时交互**：首页列表与详情页回复均采用 JavaScript Fetch API 结合定时器实现异步轮询，发现新数据自动更新 UI 或重载页面
- **格式自适应**：
    - **换行保留**：通过 CSS `white-space: pre-wrap` 确保用户发布的复杂内容格式不丢失
    - **时间戳**：后端 Reply 模型通过 `getFormattedTime()` 自动将 long 类型时间戳转换为 `yyyy-MM-dd HH:mm` 字符串
- **权限控制**：基于 HttpSession 的登录验证，确保仅登录用户可进行发布和回帖操作
- **验证码安全**：集成验证码机制防止恶意注册和登录攻击

---

## 🛠️ 技术栈

- **后端**：Java 17 / Spring Boot 3 / Spring Data JPA / Maven
- **前端**：Thymeleaf / 原生 JavaScript (ES6+) / CSS3
- **数据库**：支持多种数据库（通过JPA Repository抽象）
- **安全**：密码加密 / 会话管理 / 验证码
- **工具**：Lombok / 其他开发工具

---

## 📂 部署指南 (Linux)

### 1. 打包项目
```bash
mvn clean package -DskipTests
```

### 2. 上传 JAR 文件
将生成的 `target/Springwebaskplatform.jar` 上传至服务器 `/root` 目录。

### 3. 配置数据库
根据 `application.properties` 中的配置，确保数据库服务正常运行并创建相应的数据库。

### 4. 后台运行
```bash
nohup java -jar /root/Springwebaskplatform.jar > output.log 2>&1 &
```

### 5. 查看日志
```bash
tail -f output.log
```

### 6. 访问应用
打开浏览器访问：http://服务器IP:8080/login

---

## 🔧 开发环境配置

1. **克隆项目**
```bash
git clone <项目地址>
```

2. **导入项目**
- 使用 IntelliJ IDEA 或 Eclipse 导入 Maven 项目
- 确保已安装 Java 17 或更高版本

3. **配置数据库**
   修改 `src/main/resources/application.properties` 中的数据库连接配置

4. **运行项目**
```bash
mvn spring-boot:run
```

---

## 📄 许可证
本项目仅供学习与交流使用，遵循开源协议。
